<!--pages/申请/shengqing.wxml-->
<form bindsubmit="formSubmit" bindreset="formReset">
  <view class="detail-address section">
    <view class="section__title">二级商单位： </view>
      <view class="picker">
     <input name="inputDanwei" placeholder="输入二级商单位" value="{{detailAddress}}" />
    </view>  
  </view>

  <view class="detail-address section">
    <view class="section__title">二级商单元： </view>
      <view class="picker">
     <input name="inputDanyuan" placeholder="输入二级商单元" value="{{detailAddress}}" />
    </view>
  </view>



  <view class="detail-address section">
  	 <view class="section__title">二级商负责人：</view>
   	<view class="picker">
    	<input name="inputName" placeholder="姓名" value="{{receiverName}}" />    
    </view>	
  </view>

  <view class="detail-address section">
    <view class="section__title">联系方式：</view>
     	<view class="picker">
    <input name="inputMobile" placeholder="手机号/座机号" value="{{receiverMobile}}" />
    </view>
    </view> 
 <text> {{motto}} </text>
  <text>请关注微信公众号【bmob后端云服务平台】回复openid，将得到的代码复制粘贴到下面</text>
  <text> {{motto}} </text>

    <view class="detail-address section">
    <view class="section__title">输入openid：</view>
     	<view class="picker">
    <input name="mima" placeholder="输入openid" value="{{receivermima}}" />
    </view>
    </view>  
 

  <view class="btn-area">
    <button formType="submit" style='background:#00F5FF'>提交申请</button>
  </view>
</form>
